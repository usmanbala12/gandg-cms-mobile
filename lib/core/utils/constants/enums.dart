enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered, cancelled }

enum PaymentMethods {
  paypal,
  googlepay,
  applepay,
  visa,
  masterCard,
  creditCard,
  debitCard,
  payStack,
  cashOnDelivery,
}
